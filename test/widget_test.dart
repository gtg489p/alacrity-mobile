import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Phase 1 placeholder — real tests will be added in Phase 2+
    expect(1 + 1, 2);
  });
}
